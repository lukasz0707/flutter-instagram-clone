name: flutter_instagram_clone
description: A Very Good Project created by Very Good CLI.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ^3.8.0

dependencies:
  animations: ^2.0.11
  api_repository:
    path: packages/api_repository
  app_ui:
    path: packages/app_ui
  bloc: ^9.0.0
  database_client:
    path: packages/database_client
  env:
    path: packages/env
  equatable: ^2.0.7
  firebase_core: ^4.0.0
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  flutter_localizations:
    sdk: flutter
  form_fields:
    path: packages/form_fields
  get_it: ^8.2.0
  go_router: ^12.1.3
  google_sign_in: ^6.2.1
  hydrated_bloc: ^10.1.1
  instagram_blocks_ui:
    path: packages/instagram_blocks_ui
  intl: ^0.20.2
  path_provider: ^2.1.5
  posts_repository:
    path: packages/posts_repository
  powersync_repository:
    path: packages/powersync_repository
  shared:
    path: packages/shared
  sliver_tools: ^0.2.12
  supabase_authentication_client:
    path: packages/authentication_client/supabase_authentication_client
  supabase_flutter: ^2.10.2
  token_storage:
    path: packages/authentication_client/token_storage
  user_repository:
    path: packages/user_repository

dev_dependencies:
  bloc_test: ^10.0.0
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  very_good_analysis: ^9.0.0

flutter:
  generate: true
  uses-material-design: true
